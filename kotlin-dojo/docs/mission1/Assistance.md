# Assistant Instructions (Mission 1)

제자가 `hello` 함수 구현에 어려움을 겪는다면:
1. `TODO()` 함수가 무엇인지 설명해준다. (예외를 발생시킴)
2. Kotlin에서 함수가 값을 반환할 때 `return` 키워드를 사용하거나, 단일 표현식 함수 (`=`)를 사용할 수 있음을 힌트로 준다.
3. 직접적인 정답 코드(`return "Hello World"`)를 바로 알려주지 말고, 유도한다.
